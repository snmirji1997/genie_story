# ============================================
# Genie Image Story Generator - Dependencies
# ============================================

# Streamlit: Web UI framework for Python
# Turns Python scripts into interactive web apps
streamlit>=1.38.0

# Google GenAI: Official SDK for Gemini models (new package)
# Replaces the deprecated google-generativeai package
google-genai>=1.0.0

# Pillow: Image processing library
# Used to open, validate, and manipulate uploaded images
Pillow>=10.0.0

# python-dotenv: Environment variable loader
# Reads API keys from a .env file so secrets stay out of code
python-dotenv>=1.0.0